describe("useSpeechToTextInference integration", () => {
  // code for useSpeechToTextInference.initModel - integration tests
  // flow 1: should move from idle to ready when model loads

  // code for useSpeechToTextInference.transcribe - integration tests
  // flow 2: should parse WAV and return transcript on happy flow
  // flow 3: should expose error state when parsing/transcription fails
});
